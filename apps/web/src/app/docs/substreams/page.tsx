import { DocsPage } from "@/features/communication/docs";

export const metadata = {
  title: "Why Scout uses Substreams",
  robots: { index: true, follow: true },
};

export default function Page() {
  return <DocsPage page="substreams" />;
}
